class GameManager extends Component
{
    cameraTarget = null;

    Start()
    {

    }

    Update()
    {
        // Lerp towards target gameobject's position for smooth movement
        if (cameraTarget != null)
        {
            var newX = (0);
            var newY = (0);
        }
    }
}